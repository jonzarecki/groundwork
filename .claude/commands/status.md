Show a summary of the contacts database.

## Instructions

```bash
./scripts/status.sh
```

Show the output to the user. If the database doesn't exist, tell the user to run `./scripts/run-collect.sh` first.
